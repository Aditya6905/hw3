<h1>New Activity for <%= @place.name %></h1>

<%= form_with(model: [@place, @activity], local: true) do |form| %>
  <%= form.label :name %>
  <%= form.text_field :name %>

  <%= form.submit %>
<% end %>

<%= link_to 'Back', place_path(@place) %>