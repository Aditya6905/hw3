<h1><%= @place.name %></h1>
<h2>Activities:</h2>
<ul>
  <% @activities.each do |activity| %>
    <li><%= activity.name %></li>
  <% end %>
</ul>
<%= link_to 'New Activity', new_place_activity_path(@place) %>
<%= link_to 'Back', places_path %>